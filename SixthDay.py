#Python with Django SetUp
'''1. First we have to open cmd
2. check python version if not show then install python
3. make directory of python then check pip version
4. for pip version type "pip --version"
5. if pip version show then ok otherwise type "pip install virtualenv"
6. further type "virtualenv ."
7. then activate scripts of file
8. after install django and pillow 
9. for this type "pip install django pillow"
10. after install django and pillow, open the setup folder in the VS code
11. then develop applications in python with django'''